import { Quad, Term } from "@rdfjs/types";
import { RdfStore } from "rdf-stores";
import { Condition, Range } from "./condition.js";
export type Path = {
    store: RdfStore;
    id: Term;
};
export declare function cbdEquals(a: Path, b: Path): boolean;
type PathRange = {
    cbdEntry: Term;
    range?: Range;
};
export declare class RelationCondition {
    store: RdfStore<any, Quad>;
    ranges: PathRange[];
    constructor(store: RdfStore<any, Quad>);
    allowed(condition: Condition): boolean;
    addRelation(relationId: Term): void;
}
export {};
