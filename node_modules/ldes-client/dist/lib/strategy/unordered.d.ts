import { Manager } from "../memberManager.js";
import { Fetcher } from "../pageFetcher.js";
import { ModulatorFactory, Notifier } from "../utils.js";
import { StrategyEvents } from "./index.js";
export declare class UnorderedStrategy {
    private manager;
    private fetcher;
    private notifier;
    private inFlight;
    private fetchNotifier;
    private memberNotifier;
    private modulator;
    private cacheList;
    private polling;
    private pollInterval?;
    private cancled;
    constructor(memberManager: Manager, fetcher: Fetcher, notifier: Notifier<StrategyEvents, {}>, modulatorFactory: ModulatorFactory, polling: boolean, pollInterval?: number);
    start(url: string): void;
    cancel(): void;
    private handleFetched;
    private checkEnd;
}
