import { empty_condition as emptyCondition } from "./condition/index.js";
const defaultConfig = {
    urlIsView: false,
    noShape: false,
    condition: emptyCondition(),
    loose: false,
    polling: false,
    url: "",
    pollInterval: 200,
};
const defaultTarget = {
    target: {},
};
export async function getConfig() {
    return Object.assign({}, defaultConfig, defaultTarget);
}
export function intoConfig(config) {
    return Object.assign({}, defaultConfig, defaultTarget, config);
}
